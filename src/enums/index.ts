// Enums for the Pagination component
export enum PaginationSizesEnum {
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl',
}

export enum PaginationColorsEnum {
  primary = 'primary',
  secondary = 'secondary',
  success = 'success',
  danger = 'danger',
  warning = 'warning',
  info = 'info',
  default = 'default',
  light = 'light',
}
